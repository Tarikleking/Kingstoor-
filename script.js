document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));
const apk=document.getElementById('apk');if(apk){apk.addEventListener('click',e=>{e.preventDefault();fetch('download/app.apk',{method:'HEAD'}).then(r=>{if(r.ok)location.href='download/app.apk';else throw 0}).catch(()=>alert('ضع ملف التطبيق الحقيقي باسم app.apk داخل مجلد download ثم أعد النشر على Vercel.'))})}
const gallery=document.querySelector('.screens');
if(gallery){let down=false,startX=0,startScroll=0,moved=false;gallery.addEventListener('pointerdown',e=>{down=true;moved=false;startX=e.clientX;startScroll=gallery.scrollLeft;gallery.classList.add('dragging');gallery.setPointerCapture?.(e.pointerId)});gallery.addEventListener('pointermove',e=>{if(!down)return;const dx=e.clientX-startX;if(Math.abs(dx)>5)moved=true;gallery.scrollLeft=startScroll-dx});const stop=()=>{down=false;gallery.classList.remove('dragging')};gallery.addEventListener('pointerup',stop);gallery.addEventListener('pointercancel',stop);gallery.addEventListener('mouseleave',stop);gallery.addEventListener('click',e=>{if(moved)e.preventDefault()})}
